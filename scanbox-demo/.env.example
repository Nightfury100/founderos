# Optional. Enables free-text needs parsing (Demo 1) and label vision (Demo 2).
# Without it the app runs deterministic fallbacks and says so in the UI.
ANTHROPIC_API_KEY=

# Optional overrides
# SCANBOX_MODEL=claude-opus-4-8
# PORT=4173
# CRAWL_DELAY_MS=1500
