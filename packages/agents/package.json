{
  "name": "@founderos/agents",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "eslint . --ext .ts --max-warnings=0",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.112.4",
    "@founderos/core": "workspace:*",
    "@founderos/db": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@founderos/config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
