{
  "name": "@founderos/core",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "echo '[core] eslint wiring lands in M2; typecheck + tests are the enforced gate for now'",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rm -rf node_modules dist"
  },
  "dependencies": {
    "@founderos/db": "workspace:*",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@founderos/config": "workspace:*",
    "@types/node": "^22.10.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
